{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:vercel": "npm run dev:vercel --prefix frontend",
    "build": "npm run build --prefix frontend",
    "install:frontend": "npm install --prefix frontend"
  }
}
